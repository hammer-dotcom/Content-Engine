This folder holds every article's working files (research, outlines, drafts, previews).
It is where you go to find and fix the one step that produced a bad result. Ignored by
git by default, see .gitignore, since it's your content, not the engine's code.
